import pandas as pd


class InvestStrategyHandler(object):

    def find_expected_interest(self, df, start_date, end_date):
        for idx, row in df.iterrows():
            print(idx)
        return None
